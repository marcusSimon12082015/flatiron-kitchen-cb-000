<h1>Recipe Edit:</h1>
<%= form_for @recipe, url:{controller: 'recipe', action:'update'}, html:{method:'put'} do |f| %>
  <%= f.text_field :name %>
  <%= f.collection_check_boxes :ingredient_ids, Ingredient.all, :id, :name %><br>
  <%= f.submit "Update Recipe" %>
<% end %>
